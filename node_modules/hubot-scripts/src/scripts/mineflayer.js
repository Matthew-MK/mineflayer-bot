// Description:
//   Mineflayer bot access from Hubot
//
// Dependencies:
//   None
//
// Configuration:
//   HUBOT_MINECRAFT_HOST  - Minecraft server to connect to
//   HUBOT_MINECRAFT_PORT  - Port of Minecraft server to connecto
//   HUBOT_MINECRAFT_USERNAME  - Username of Minecraft account that the bot will use
//   HUBOT_MINECRAFT_PASSWORD  - Password of Minecraft account that the bot will use
//
// Commands:
//
// Author:
//   eliperelman

module.exports = function (robot) {

    // in here you will configure robot listeners

    // for example if you want to listen for whenever someone says:
    // jarvis gamemode me creative
    // jarvis gamemode me survival
    // jarvis gamemode me 0
    // jarvis gamemode me c
    // etc.

    // you can register a listener like so:
    // (note: all listeners take a regular express and a function
    // to execute if the robot hears the expression)

    robot.respond(/gamemode me (.*)/i, function (message) {
        // message is an object containing data about the request
        // message.match is an array of words said that match the regular expression
        // for example, if the user had said: "jarvis gamemode me creative",
        // message.match[0] would be "gamemode me creative", and message.match[1] would be "creative"

        // use robot.bot to interact with the mineflayer API, which can make the
        // robot do or interact with the world:
        // https://github.com/superjoe30/mineflayer/blob/master/doc/api.md

        var player = message.robot.bot.players[message.message.user.id];


        // when you are done, you can choose to respond,
        // either in global chat or directly to the user
        // use message.send('something'); to chat in global chat
        // use message.reply('something'); to /msg the user privately
    });

};